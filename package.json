{
  "name": "tiina-portfolio",
  "private": true,
  "scripts": {
    "dev:backend": "backend\\.venv\\Scripts\\python.exe -m uvicorn app.main:app --app-dir backend --reload --reload-dir backend --reload-dir me",
    "dev:frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build"
  }
}
